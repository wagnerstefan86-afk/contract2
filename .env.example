# PostgreSQL
POSTGRES_USER=contract_review
POSTGRES_PASSWORD=changeme
POSTGRES_DB=contract_review

# Backend
DATABASE_URL=postgresql+asyncpg://contract_review:changeme@db:5432/contract_review
UPLOAD_DIR=/data/uploads
SECRET_KEY=changeme-generate-a-real-secret

# LLM (optional — can also be set via Einstellungen UI)
# LLM_PROVIDER=openai
# LLM_MODEL=gpt-4o
# LLM_API_KEY=sk-...
# LLM_BASE_URL=
